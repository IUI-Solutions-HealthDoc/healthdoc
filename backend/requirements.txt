# Verified against pip-audit on 26 Aug 2026: "No known vulnerabilities found".
# WASA requires zero active unpatched CVEs, so re-run before booking the audit:
#
#     pip-audit -r requirements.txt
#
# The four packages below carry a lower bound rather than the repo's usual
# ==X.Y.* pin, because each bound is the version that CLOSES a CVE. Pinning to
# a minor series would silently re-open them the next time the series is
# rebuilt. Upper bounds stay off deliberately: a cap here is a future CVE.
fastapi>=0.141.1
uvicorn[standard]==0.30.*
pydantic[email]==2.*
pydantic-settings==2.*
sqlalchemy[asyncio]==2.0.*
asyncpg==0.29.*
psycopg2-binary==2.9.*
alembic==1.13.*
motor==3.5.*
redis==5.*
httpx==0.27.*

# Replaces python-jose[cryptography]==3.3.*, which verified every access token
# in app/auth/deps.py and carried three advisories — including PYSEC-2025-185,
# which has NO published fix. The project is effectively unmaintained, so no
# version bump could have closed it. python-jose also dragged in `ecdsa`, whose
# PYSEC-2026-1325 is likewise unfixed; dropping the parent drops both.
#
# 2.13.0 is the floor: 2.10.1 carries five advisories of its own.
PyJWT[crypto]>=2.13.0

cryptography>=42
minio==7.2.*

# 0.0.9 carried seven advisories. This parses multipart file uploads, which is
# an unauthenticated-shaped attack surface in any app that accepts them.
python-multipart>=0.0.32

# Starlette 0.46.2 carried seven advisories; 1.3.1 is the floor that clears
# them all. FastAPI pins starlette, so the fastapi bound above is what actually
# delivers this — it is named explicitly so pip-audit's finding maps to a line.
starlette>=1.6.0

prometheus-client==0.26.*
